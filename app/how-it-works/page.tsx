export default function Page(){let s=[["1. Verify the teacher","Identity and current school employment are privately reviewed by GBTS."],["2. Post a need","A verified teacher submits a physical item, quantity, specification, purpose and estimated price."],["3. Giver commits","A giver chooses an approved need and commits to provide it."],["4. Giver buys it","The giver purchases the compliant item from a supplier of their choice. GBTS handles no donation money."],["5. Deliver and confirm","Evidence is uploaded, the teacher confirms receipt, and the project is completed."]];return <main className="page"><div className="eyebrow">Locked MVP workflow</div><h1>How it works</h1>{s.map(x=><div className="card" style={{marginBottom:14}} key={x[0]}><h3>{x[0]}</h3><p className="muted">{x[1]}</p></div>)}</main>}